syntax = "proto3";

package club.subjugated.proto.message;;

message ClientHello {
  string name = 1;
}

message AuthChallenge {
  string name = 1;
  bytes nonce = 2; // ECDSA signatures typically use 32-byte nonces (for secp256r1 and secp256k1)
}
